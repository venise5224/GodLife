function Footer() {
  return (
    <footer className="fixed bottom-0 left-0 w-full p-4 bg-white shadow flex justify-around">
      <button>홈</button>
      <button>통계</button>
      <button>설정</button>
    </footer>
  );
}

export default Footer;
